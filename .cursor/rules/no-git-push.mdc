---
description: Never run git push unless the user explicitly asks for it
alwaysApply: true
---

# No Git Push

Never run `git push` or any variant (`git push origin`, `git push -u`, `git push --force`, etc.) unless the user explicitly says "push" or "push at".

Commits are fine when requested, but pushing to remote requires explicit user approval every time.
